# UI assets

Optional background image for the themed layout:

- **`shrine-bg.jpg`** — If present, it is used as the full-screen background (ruined shrine / dark fantasy). If missing, the app uses a CSS gradient fallback; no image is required.
